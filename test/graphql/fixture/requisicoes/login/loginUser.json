{
    "query": "mutation Login($username: String!, $password: String!) { loginUser(username: $username, password: $password) { token } }",
    "variables": {
        "username": "julio",
        "password": "123456"
    }
}